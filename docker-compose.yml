version: "3.8"

services:
  # Banco de dados PostgreSQL
  db:
    image: postgres:15
    container_name: hineni_db
    restart: always
    environment:
      POSTGRES_USER: hineni
      POSTGRES_PASSWORD: hineni123
      POSTGRES_DB: hineni_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Interface visual para gerenciar o banco (acesse: http://localhost:5050)
  pgadmin:
    image: dpage/pgadmin4
    container_name: hineni_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@hineni.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - db

volumes:
  postgres_data:
